# DoH-rfc8484-Batch-Resolver
A lightweight Python script that resolves domain names via **DNS-over-HTTPS (DoH)** as defined in **RFC 8484**, with multi-server failover and IPv4/IPv6 preference control.
